# Skill: 02-spec-add-users-ddl-note

**Goal:** Append a short **development-only** “Users table” SQL snippet to `implementation.md` (or a new `docs/specs/sql.md`), so team members can quickly create the minimal DB for Phase B.  
**Docs only; no code.**

## Files allowed to change
- docs/specs/implementation.md  *(append-only section “Dev SQL — Users table”)*

## Guardrails
- Append-only. Do not edit other sections.
- No code changes.

## Prompt template
```
Skill: 02-spec-add-users-ddl-note
Goal: Append a small dev-only SQL snippet for dbo.Users to docs/specs/implementation.md.
Files allowed: docs/specs/implementation.md (append-only)
Rules: Append a new section "Dev SQL — Users table". Do not modify other content.
```
